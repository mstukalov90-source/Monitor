# Local PostGIS
# Use strong secrets in production
POSTGRES_DB=monitor
POSTGRES_USER=monitor
POSTGRES_PASSWORD=ChangeMe_StrongPassword_32+
POSTGRES_PORT=5432

# Remote SPS database (lens sync at 04:00)
REMOTE_DB_HOST=172.16.206.170
REMOTE_DB_PORT=5432
REMOTE_DB_NAME=sps
REMOTE_DB_USER=asidorov
REMOTE_DB_PASSWORD=qwerty1234

# data.mos.ru API key (optional, overrides keys in data_mos_export_*.py)
DATA_MOS_API_KEY=

# Timezone for cron schedule
TZ=Europe/Moscow

# Security note:
# PostgreSQL is bound to 127.0.0.1 in docker-compose.
# For external admin access use SSH tunnel, for example:
# ssh -i <private_key> -L 5432:127.0.0.1:5432 root@<server_ip>
